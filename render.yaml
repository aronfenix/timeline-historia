services:
  - type: web
    name: timeline-historia
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /var/data
    disk:
      name: timeline-historia-data
      mountPath: /var/data
      sizeGB: 1
