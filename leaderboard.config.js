// Optional shared leaderboard backend.
// Set baseUrl to the deployed server URL, for example:
// window.TIMELINE_ONLINE_API = { baseUrl: 'https://timeline-historia-api.onrender.com' };
window.TIMELINE_ONLINE_API = window.TIMELINE_ONLINE_API || {
  baseUrl: ''
};
